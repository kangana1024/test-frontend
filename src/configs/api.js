export const host = 'http://localhost';
export const port = ':3001';
export const baseUrl = host + port;
export const authApi = baseUrl + '/users/signin';
export const sendMail = baseUrl + '/sendmail';
export const groupContactApi = baseUrl + '/contacts/group';
